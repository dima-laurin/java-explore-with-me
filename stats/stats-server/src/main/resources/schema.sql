CREATE TABLE IF NOT EXISTS endpoint_hits (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    app VARCHAR(500) NOT NULL,
    uri VARCHAR(5000) NOT NULL,
    ip VARCHAR(500) NOT NULL,
    timestamp_hit TIMESTAMP NOT NULL
);